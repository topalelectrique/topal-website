// Run this once to add pair_id column to articles table
// Go to Supabase SQL Editor and run:
// ALTER TABLE articles ADD COLUMN pair_id UUID;
// ALTER TABLE articles ADD COLUMN reading_time INT DEFAULT 5;

console.log('Run this SQL in Supabase SQL Editor:');
console.log('');
console.log('ALTER TABLE articles ADD COLUMN pair_id UUID;');
console.log('ALTER TABLE articles ADD COLUMN reading_time INT DEFAULT 5;');
